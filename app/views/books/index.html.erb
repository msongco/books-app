<div class="w-8/12 p-4 mx-auto mt-6 border">
    <div class="flex flex-col">
        <% @books.each do |book| %>
            <%= render partial: "book", locals: { book: book } %>
        <% end %>
    </div>
</div>